#include <iostream>
#include <vector>
#include <string>
using std::cin; using std::cout; using std::endl;
using std::string; using std::vector;

int prac_318()
{
	vector<int> ivec;
	if (!ivec.empty())
		ivec[0] = 42;

	return 0;
}