#include <iostream>

using std::cin; using std::cout; using std::endl;

int array01_36()
{
	//多维数组  数组的数组
	int ia[3][4];   //大小为3的数组，每个元素还有4个整数的数组
	int arr[10][20][30] = { 0 }; //大小为10的数组，每个元素都是大小有20的数组，这些数组的元素是含有30个整数的数组

	//二维数组，第一个数 是行，第二个是列；

	return 0;
	
}