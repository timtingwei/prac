#include<iostream>
#include<string>
#include<vector>
#include"cassert"
using std::begin; using std::endl;
using std::cin; using std::cout; using std::endl;
using std::string;
using std::vector;


/*void ff(int);
void ff(short);

//所有算术类型转换的级别都一样
void manip(long);
void manip(float);

//非常量对象转换为常量对象引用需要转换类型*/

/*//如果实参是指向常量的指针，调用形参是const*的函数；如果实参是指向非常量的指针，调用形参是普通指针的函数
int main_conversion661()
{
	int a = 5;
	ff(a);
	ff('a');      //char提升成int，调用ff(int)，小整型一般会被提升成int类型或者更大

	//manip(3.14);   //错误：二异性调用，double转换成long和float是在同一个等级的。
	system("pause");
	return 0;
}*/