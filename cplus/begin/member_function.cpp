#include <iostream>
/*#include "Sales_item.h"
int member_function()
{
	Sales_item item1, item2;
	std::cin >> item1 >> item2;
	//首先检查item1和item2是否表示相同的书
	if (item1.isbn() == item2.isbn()){						// . 运算符只能用于类类型的对象，左侧是对象，右侧是成员名，运算结果是指定成员名的成员
		//调用运算符是()，里面放着argument，可能为空
		std::cout << item1 + item2 << std::endl;
		
		return 0;
	}
	else {
		std::cerr << "Data must refer to same ISBN" << std::endl;
		system("pause");
		return -1;
	}
}*/