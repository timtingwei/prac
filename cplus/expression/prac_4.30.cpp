#include <iostream>
#include <vector>
#include <string>

using std::cin; using std::cout; using std::endl;
using std::vector;
using std::string;

int prac_430()
{
	/*//根据优先级和结合律，在下述表达式适当位置加上括号，与原来含义相同
	sizeof x + y;
	//优先级：sizeof > +，sizeof x运算结果作为 +运算符的左侧对象，与y相加，返回值可能是int
	//(sizeof x) + y;
	sizeof p->mem[i];
	//优先级：->,[] > sizeof,size,先访问指针p所指向元素的成员的第i个值，该值作为sizeof的运算对象，返回该值类型
	//(sizeof (p->(mem[i]))); 
	sizeof a < b;
	//优先级 sizeof > <;先得到a的类型所占用空间大小，然后转换成bool值，作为<左侧运算对象和b进行比较,返回bool值
	//(sizeof a) < b;
	sizeof f();
	//优先级 () > sizeof;先得到f()的返回值，作为sizeof的运算对象，返回该值所占的空间大小*/

	system("pause");
	return 0;


}